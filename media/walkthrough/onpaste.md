# Replace on paste

When enabled, pasted text has smart characters cleaned as it lands in the editor. Only triggers when the pasted text actually contains targeted characters - normal pastes are unaffected.

Useful for pasting from Word, Google Docs, web pages, or LLM output.

Toggle later via _Settings > Dumb Formatter > On Paste: Enabled_.
